const Discord = require("discord.js");

module.exports = {
  name: "",
  description: "",
  group: 'fun',
  cooldown: 1,
  guildOnly: true,
  execute: async (message, args, bot, config, command, aargs) => {
    
  }
};